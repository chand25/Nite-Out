 handleScroll(event) {
    if(($(document).scrollTop() + $(window).height() / 2) > ($('body').height() / 2)){
      $('#addType').addClass( 'innerStory' );
    }
  };

  document.querySelector('#addType').

   document.querySelector('.aboutContainer').onscroll =
      function() {
        console.log("scrolling");
      };
      $('#addType').addClass( 'innerStory' );
    };


$(window).scroll(function(){
      //document.querySelector('.aboutContainer').onscroll =
      //function() {
    if(($(document).scrollTop() + $(window).height() / 2) > ($('body').height() / 2)){
      $('#addType').addClass( 'innerStory' );
       // $('.homecontent').addClass( 'innerStory' );
        console.log("scrolling")
    }
  });
